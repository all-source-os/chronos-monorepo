# Verified SEO Action Plan

## Immediate

1. Resubmit `sitemap.xml` in Google Search Console. Current XML and every listed URL
   pass live validation.

## P1: next engineering pass

1. Move design-partner banner dismissal from server `cookies()` to client-local
   storage or isolate dynamic state so public routes regain cacheability.
2. Replace globally repeated schema with canonical Brand, Wolven Tech Organization,
   Person, WebSite, and complete SoftwareApplication graph on relevant pages only.
3. Fix stale public-source claims in package READMEs, Docs.rs releases, repository
   MCP badge/counts, and website copy.
4. Fix four WCAG contrast failures and investigate broken Vercel Analytics script.
5. Configure one-hop permanent apex/HTTP redirects to canonical HTTPS www host.

## P2: next content pass

1. Rewrite eight highest-impact metadata outliers listed in verified report.
2. Add contextual internal links to agent-memory articles; establish
   `/solutions/agent-memory` as commercial hub.
3. Replace comparison cards with accessible matrix plus primary citations.
4. Suppress or move universal design-partner banner on docs and intent-specific pages.
5. Make article modified dates visible and add primary sources to uncited posts.
6. Add page-specific OG assets and selected inline diagrams where they improve proof.

## Backlog

- Remove expired literal date from `llms-full.txt` sample.
- Add lazy loading to below-fold homepage article images.
- Evaluate IndexNow only if Bing discovery speed becomes important.
- Harden CSP with nonce/hash migration after framework-compatible testing.
