{{- if .Values.queryService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "allsource.queryService.fullname" . }}
  labels:
    {{- include "allsource.queryService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.queryService.service.type }}
  ports:
    - port: {{ .Values.queryService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allsource.queryService.selectorLabels" . | nindent 4 }}
{{- end }}
