{
  "name": "@allsource/control-plane",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/control-plane main.go",
    "clean": "rm -rf bin",
    "lint": "go vet ./..."
  }
}
