# Override for local development - unique names and ports in 3900-3999 range
services:
  core:
    container_name: chronos-monorepo-core
    ports:
      - "3900:3900"

  control-plane:
    container_name: chronos-monorepo-control-plane
    ports:
      - "3901:8080"

  query-service:
    container_name: chronos-monorepo-query-service
    ports:
      - "3902:3902"

  postgres:
    container_name: chronos-monorepo-postgres
    ports:
      - "3903:5432"

  web:
    container_name: chronos-monorepo-web
    ports:
      - "3908:3000"

  redis:
    container_name: chronos-monorepo-redis
    ports:
      - "3905:6379"

  prometheus:
    container_name: chronos-monorepo-prometheus
    ports:
      - "3906:9090"

  grafana:
    container_name: chronos-monorepo-grafana
    ports:
      - "3907:3000"

networks:
  chronos:
    name: chronos-monorepo-network

volumes:
  core-data:
    name: chronos-monorepo-core-data
  postgres-data:
    name: chronos-monorepo-postgres-data
  redis-data:
    name: chronos-monorepo-redis-data
  prometheus-data:
    name: chronos-monorepo-prometheus-data
  grafana-data:
    name: chronos-monorepo-grafana-data
