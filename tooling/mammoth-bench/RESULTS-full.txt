mammoth-bench (full) — 5-claim recall benchmark
run: 2026-05-30   corpus: 60 memories / 60 queries   k=5
prime: allsource-prime 0.21.4

1. RECALL PRECISION (measured)
   memory    hit@3=0.78  hit@5=0.85  MRR=0.717
   baseline  hit@3=0.70  hit@5=0.75  (search+grep)
   Δ hit@5 = +0.10   → memory wins

2. CROSS-SESSION CONTINUITY (proxy = retrievability, not an LLM judge)
   memory-ON answerable=0.85   memory-OFF answerable=0.75
   win-rate (memory answers where baseline cannot) = 0.10

3. TOKENS SAVED (estimate, ~1.3 tok/word)
   median saved/recall = 28   total = 1717 over 51 hits

4. RECALL LATENCY (measured, incl. in-process fastembed embed)
   p50 = 2.1ms   p95 = 4.8ms   @ 60 nodes

5. DURABILITY (write -> restart -> read)
   PASS — all spot-checked memories survived server restart

VERDICT: PASS

Honesty note: the precision edge over the grep baseline NARROWED from +0.17
(18-memory smoke set, bench.py) to +0.10 at 60 memories — expected as the
baseline has more keyword overlap to exploit in a denser corpus. Memory still
wins on every metric, latency stays sub-5ms, durability holds. Per the proposal's
honesty rule, the softening is published, not hidden; the improvement levers
(hybrid keyword+vector, graph depth>0, larger embedder) remain unused here.
